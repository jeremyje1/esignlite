# App
APP_URL=http://localhost:3000
JWT_SECRET=<long-random-string>            # used for signer magic links

# Database (Prisma - SQLite dev)
DATABASE_URL="file:./prisma/dev.db"

# Email (SendGrid)
SENDGRID_API_KEY=<sendgrid_api_key>
MAIL_FROM=<from_email>

# Supabase (public client)
NEXT_PUBLIC_SUPABASE_URL=<supabase_project_url>
NEXT_PUBLIC_SUPABASE_ANON_KEY=<supabase_anon_public_key>

# Supabase (server-only, optional, for future use)
SUPABASE_SERVICE_ROLE_KEY=<service_role_if_used_later>
SUPABASE_JWT_SECRET=<supabase_jwt_secret>  # if verifying supabase auth tokens server-side
